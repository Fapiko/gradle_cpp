project(":print_helper") {
	apply plugin: "cpp-lib"
}

project(':test_binary') {
	apply plugin: 'cpp-exe'
	cpp {
		sourceSets {
			main {
				libs << project(':print_helper').libraries.main
			}
		}
	}
}
